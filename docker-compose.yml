services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=dev-secret-change-me
      - ADMIN_PASSWORD=admin
      - DB_PATH=/data/vault.db
      - UPLOAD_DIR=/data/uploads
      - SENTRY_DSN=https://1d5556927867eb5d24212d5a5edcee91@o4510562690859008.ingest.de.sentry.io/4510896156377168
    volumes:
      - vault-data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./frontend/nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"
    restart: unless-stopped

volumes:
  vault-data:
